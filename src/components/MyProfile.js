import React from 'react';
import {
  Container, Row,
} from 'react-bootstrap';
import Mission from './missions';

const MyProfile = () => (
  <Container>
    <Row>
      <Mission />
    </Row>
  </Container>
);

export default MyProfile;
